import { MousePosition } from './event-input';

export default class ContourController {
    public handleMouseMove(holder: DeformerHolderElement, positions: MousePosition[]) {
        // TODO
    }
}
